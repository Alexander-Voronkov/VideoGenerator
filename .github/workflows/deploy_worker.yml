name: Deploy .NET App via SSH (Rebuild Container)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy updated source code to remote server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.VM_SECRET_KEY }}
          source: "."
          target: "~/videogeneratorworker-src"

      - name: Rebuild and restart container on remote server
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.VM_SECRET_KEY }}
          port: 22
          script: |
            echo "🚀 Starting deploy on $(hostname)"
            cd ~/videogeneratorworker-src

            echo "🔨 Rebuilding Docker image..."
            sudo docker build -t videogeneratorworker:latest .

            if sudo docker ps -q -f name=videogeneratorworker > /dev/null; then
              echo "♻️ Restarting existing container..."
              sudo docker stop videogeneratorworker
              sudo docker rm videogeneratorworker
            else
              echo "🆕 No existing container found, creating new one..."
            fi

            echo "🔥 Running updated container..."
            sudo docker run -d \
              --name videogeneratorworker \
              --restart always \
              --network npmnetwork \
              -e DOTNET_ENVIRONMENT=Production \
              videogeneratorworker:latest

            echo "✅ Deploy complete!"
